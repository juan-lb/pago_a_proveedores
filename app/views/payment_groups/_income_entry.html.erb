<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= f.label 'Cuenta crédito',
      class: 'control-label'
    %>
    <%= f.select :bank_account,
      Account.all_for_fake_payments.map { |each|
        [
          each['nom_cue'].strip,
          each['id_cue'],
        ]
      },
      {include_blank: 'Elija una cuenta'},
      {class: 'form-control'}
    %>
  </div>
</div>
