<tr class="js-account">
  <!-- nombre -->
  <td><%= reserve[:file_number] %></td>

  <!-- monto en dólares -->
  <td class="js-USD-col js-currency-col">
    <%= number_to_currency reserve[:usd], unit: '' %>
  </td>

  <!-- monto en pesos -->
  <td class="js-ARS-col js-currency-col">
    <%= number_to_currency reserve[:ars], unit: '' %>
  </td>

  <!-- operadores -->
  <td>
    <%= reserve[:operators] ? reserve[:operators].to_sentence : '-' %>
  </td>
</tr>
